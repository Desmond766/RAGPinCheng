# Build context is the repo root. The frontend build only needs:
#   frontend/   — source code, package.json, etc.
#   docker/nginx.conf — nginx config copied into the nginx stage

# Exclude everything, then allowlist what we need.
*

!frontend/
frontend/node_modules/
frontend/dist/
frontend/.vite/

!docker/
docker/*
!docker/nginx.conf
