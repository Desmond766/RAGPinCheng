问答加入上下文
回答参考应该包含具体的文件名以及具体页数
加速回答速度

并发问题（两个用户同时提问时会出问题）：

BGE-M3 嵌入模型 + BGE-reranker-v2-m3 交叉编码器是进程内共享单例
   - src/embed.py 现在用 threading.Lock 串行化 model.encode()，避免 MPS 上的崩溃；
     但代价是并发推理排队执行（5 个用户大约多 ~500ms 尾延迟）。
   - rerank 仍然走 GIL + 单 CUDA 流，吞吐受限（~100–300ms × N 用户）。
   - 改进方向：micro-batching（聚合 ~20ms 窗口内的并发请求批量推理），
     或把 embed/rerank 拆成独立服务（text-embeddings-inference / Triton 等）。